const {
    User
} = require('./user');
const {
    Place
} = require('./place');
const {
    Booking
} = require('./booking');

const {
    Review
} = require('./reviews');

const {
    UserInfo
} = require('./userinfo');

const {
    Issue
} = require('./issue');

module.exports = {
    User,
    Place,
    Booking,
    UserInfo,
    Review,
    issue
}