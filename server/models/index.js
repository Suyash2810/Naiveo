const {
    User
} = require('./user');
const {
    Place
} = require('./place');
const {
    Booking
} = require('./booking');

const {
    Review
} = require('./reviews');

const {
    UserInfo
} = require('./userinfo');

module.exports = {
    User,
    Place,
    Booking,
    UserInfo,
    Review
}